---
title: "System Character"
description: "Learn how to use preset digital character avatars in real-time interactions"
---

1. [Real-time digital human API](/api/real-time-digital-human-api)

NavTalk provides a diverse collection of preset digital character avatars. When establishing a WebSocket connection, you can easily specify your preferred character through URL parameters. The system will automatically load the corresponding character model and animations.

## **Configuration Method**

Copy

```
// Construct URL with license and character parameters
const websocketUrl = 'wss://api.navtalk.ai/api/realtime-api';
const websocketUrlWithParams = `${websocketUrl}?license=${encodeURIComponent(license)}&characterName=${encodeURIComponent(characterPngName)}`;

// Establish WebSocket connection
const socket = new WebSocket(websocketUrlWithParams);
```

## **Parameter Explanation**

Parameter

Type

Required

Description

`license`

string

✅

Your API license key for authentication

`characterName`

string

✅

Preset character identifier (e.g., "girl2", "boy1")

## **System Supported Characters**

### Alex {#code-example-alex}

![Alex](/images/api/real-time-digital-human-api/Alex.png)

**Character Name:** `navtalk.Alex`

<CodeGroup>
```javascript JavaScript
const websocketUrl = 'wss://api.navtalk.ai/api/realtime-api';
const license = 'your-license-key';
const characterName = 'navtalk.Alex';
const wsUrl = `${websocketUrl}?license=${encodeURIComponent(license)}&characterName=${encodeURIComponent(characterName)}`;
const socket = new WebSocket(wsUrl);
```
</CodeGroup>

---

### Ethan {#code-example-ethan}

![Ethan](/images/api/real-time-digital-human-api/Ethan.png)

**Character Name:** `navtalk.Ethan`

<CodeGroup>
```javascript JavaScript
const websocketUrl = 'wss://api.navtalk.ai/api/realtime-api';
const license = 'your-license-key';
const characterName = 'navtalk.Ethan';
const wsUrl = `${websocketUrl}?license=${encodeURIComponent(license)}&characterName=${encodeURIComponent(characterName)}`;
const socket = new WebSocket(wsUrl);
```
</CodeGroup>

---

### Leo {#code-example-leo}

![Leo](/images/api/real-time-digital-human-api/Leo.png)

**Character Name:** `navtalk.Leo`

<CodeGroup>
```javascript JavaScript
const websocketUrl = 'wss://api.navtalk.ai/api/realtime-api';
const license = 'your-license-key';
const characterName = 'navtalk.Leo';
const wsUrl = `${websocketUrl}?license=${encodeURIComponent(license)}&characterName=${encodeURIComponent(characterName)}`;
const socket = new WebSocket(wsUrl);
```
</CodeGroup>

---

### Lily {#code-example-lily}

![Lily](/images/api/real-time-digital-human-api/Lily.png)

**Character Name:** `navtalk.Lily`

<CodeGroup>
```javascript JavaScript
const websocketUrl = 'wss://api.navtalk.ai/api/realtime-api';
const license = 'your-license-key';
const characterName = 'navtalk.Lily';
const wsUrl = `${websocketUrl}?license=${encodeURIComponent(license)}&characterName=${encodeURIComponent(characterName)}`;
const socket = new WebSocket(wsUrl);
```
</CodeGroup>

---

### Emma {#code-example-emma}

![Emma](/images/api/real-time-digital-human-api/Emma.png)

**Character Name:** `navtalk.Emma`

<CodeGroup>
```javascript JavaScript
const websocketUrl = 'wss://api.navtalk.ai/api/realtime-api';
const license = 'your-license-key';
const characterName = 'navtalk.Emma';
const wsUrl = `${websocketUrl}?license=${encodeURIComponent(license)}&characterName=${encodeURIComponent(characterName)}`;
const socket = new WebSocket(wsUrl);
```
</CodeGroup>

---

### Sophia {#code-example-sophia}

![Sophia](/images/api/real-time-digital-human-api/Sophia.png)

**Character Name:** `navtalk.Sophia`

<CodeGroup>
```javascript JavaScript
const websocketUrl = 'wss://api.navtalk.ai/api/realtime-api';
const license = 'your-license-key';
const characterName = 'navtalk.Sophia';
const wsUrl = `${websocketUrl}?license=${encodeURIComponent(license)}&characterName=${encodeURIComponent(characterName)}`;
const socket = new WebSocket(wsUrl);
```
</CodeGroup>

---

### Mia {#code-example-mia}

![Mia](/images/api/real-time-digital-human-api/Mia.png)

**Character Name:** `navtalk.Mia`

<CodeGroup>
```javascript JavaScript
const websocketUrl = 'wss://api.navtalk.ai/api/realtime-api';
const license = 'your-license-key';
const characterName = 'navtalk.Mia';
const wsUrl = `${websocketUrl}?license=${encodeURIComponent(license)}&characterName=${encodeURIComponent(characterName)}`;
const socket = new WebSocket(wsUrl);
```
</CodeGroup>

---

### Chloe {#code-example-chloe}

![Chloe](/images/api/real-time-digital-human-api/Chloe.png)

**Character Name:** `navtalk.Chloe`

<CodeGroup>
```javascript JavaScript
const websocketUrl = 'wss://api.navtalk.ai/api/realtime-api';
const license = 'your-license-key';
const characterName = 'navtalk.Chloe';
const wsUrl = `${websocketUrl}?license=${encodeURIComponent(license)}&characterName=${encodeURIComponent(characterName)}`;
const socket = new WebSocket(wsUrl);
```
</CodeGroup>

---

### Zoe {#code-example-zoe}

![Zoe](/images/api/real-time-digital-human-api/Zoe.png)

**Character Name:** `navtalk.Zoe`

<CodeGroup>
```javascript JavaScript
const websocketUrl = 'wss://api.navtalk.ai/api/realtime-api';
const license = 'your-license-key';
const characterName = 'navtalk.Zoe';
const wsUrl = `${websocketUrl}?license=${encodeURIComponent(license)}&characterName=${encodeURIComponent(characterName)}`;
const socket = new WebSocket(wsUrl);
```
</CodeGroup>

---

### Ava {#code-example-ava}

![Ava](/images/api/real-time-digital-human-api/Ava.png)

**Character Name:** `navtalk.Ava`

<CodeGroup>
```javascript JavaScript
const websocketUrl = 'wss://api.navtalk.ai/api/realtime-api';
const license = 'your-license-key';
const characterName = 'navtalk.Ava';
const wsUrl = `${websocketUrl}?license=${encodeURIComponent(license)}&characterName=${encodeURIComponent(characterName)}`;
const socket = new WebSocket(wsUrl);
```
</CodeGroup>

---

> Stay tuned for more characters!

